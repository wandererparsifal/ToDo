<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="AppTheme.Blue">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Actionbar color -->
        <item name="colorPrimary">@color/colorRoyalBlue</item>
        <!--Status bar color-->
        <item name="colorPrimaryDark">@color/colorDarkBlue</item>
        <!--Window color-->
        <item name="android:windowBackground">@color/bright_foreground_inverse_material_light</item>
    </style>

    <style name="AppTheme.Pink">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Actionbar color -->
        <item name="colorPrimary">@color/colorHotPink</item>
        <!--Status bar color-->
        <item name="colorPrimaryDark">@color/colorAccent</item>
        <!--Window color-->
        <item name="android:windowBackground">@color/bright_foreground_inverse_material_light</item>
    </style>
</resources>
